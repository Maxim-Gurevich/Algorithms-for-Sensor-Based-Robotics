function [config_1,config_2,config_3,config_4] = configuration_calculator(screw,theta,Tinit)
%UNTITLED Summary of this function goes here
%   Screw format is [[w],v;0,0] : theta is in radians

theta1 = theta/4;
theta2 = theta/2;
theta3 = (3/4)*theta;
theta4 = theta;

w = screw(1:3,1:3);
v = screw(1:3,4);
T1 = [exp(w*theta1),(eye(3)*theta1+(1-cos(theta1))*w+(theta-sin(theta))*w*w)*v;[0,0,0,1]];


config_1 = screw;
config_2 = screw;
config_3 = theta;
config_4 = theta;
end

